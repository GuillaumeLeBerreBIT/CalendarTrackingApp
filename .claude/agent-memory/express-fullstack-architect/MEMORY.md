# Agent Memory Index

## Project

- [Testing setup, Vitest config, ESM mock path trap](./project_testing.md)
  — `tests/` is a sibling of `db/` and `utils/`; always use `../` to reach peer dirs from test files
